let world : string = "Wor"+"ld";
initial {
  let world1 : string = "";
  {
    let world1 : string = world + "!";
    hello(world1);
  }
}

fun hello(name : string) -> void {
  println("Hello, " + name);
}

