val world : string = "world";
initial {
  var world1 : string = "";
  {
    var world1 : string = world + "!";
    hello(world);
  }
}

fun hello(val name : string) -> void {
  print("Hello, " + name);
}

class Test{}

namespace std {
val world : string = "world";
initial {
  var world1 : string = "";
  {
    var world1 : string = world + "!";
    hello(world);
  }
}

fun hello(val name : string) -> void {
  print("Hello, " + name);
}
}