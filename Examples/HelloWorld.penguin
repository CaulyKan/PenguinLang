let world : string = "wor"+"ld";
initial {
  let world1 : string = "";
  {
    let world1 : string = world + "!";
    hello(world1);
  }
}
fun hello(let name : string) -> void {
  println("Hello, " + name);
}