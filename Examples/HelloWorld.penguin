const world : string = "wor"+"ld";
initial {
  var world1 : string = "";
  {
    var world1 : string = world + "!";
    hello(world1);
  }
}
fun hello(const name : string) -> void {
  println("Hello, " + name);
}
