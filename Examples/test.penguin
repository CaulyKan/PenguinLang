                event test_event : i32;

                on test_event (val b: i32) {
                    print(b as string);
                    if (b == 2) exit(0);
                }
                
                initial {
                    for (var i : i64 in range(0, 3)) {
                        emit test_event(i as i32);
                    }
                }